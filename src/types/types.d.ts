export interface Characters {
  id: string;
  name: string;
  image: string;
}

export interface ListPokemon {
  name: string;
  url: string;
}

export interface DataColor {
  id: string;
  name: string;
  color: string;
  image: string;
}
